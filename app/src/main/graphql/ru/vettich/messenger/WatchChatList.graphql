subscription WatchChatList($token: String!) {
    watchChatList(token: $token) {
        id
        name
        preview {
            text
            createdAt
            user {
                id
                username
            }
        }
    }
}