subscription WatchNewMessagesInChat($token: String!, $chatId: String!) {
    watchNewMessagesInChat(token: $token, chatId: $chatId) {
        id
        text
        user {
            id
            username
        }
        createdAt
    }
}